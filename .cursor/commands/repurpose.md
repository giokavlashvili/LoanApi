---
description: Replace sample loan domain when starting a new product
---

Repurpose this boilerplate for a new product using the **repurpose-boilerplate** skill (`.cursor/skills/repurpose-boilerplate/SKILL.md`).

1. Read the skill and follow its procedure checklist.
2. Confirm with the user which sample slices to remove and the new domain name before deleting migrations.
3. Do not rename `Domain`/`Application`/`Infrastructure`/`WebApi` assemblies unless explicitly requested.
